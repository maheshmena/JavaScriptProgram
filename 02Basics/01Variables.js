// In programming, data types define what kind of data a variable can hold.

// String ... Boolean ... Number ... Null ... Undefined ... Object ... Symbol

// Declare a string variable
var fullName = "Mena Mahesh";

// Declare a boolean variable
var isLoggedIn = false; 

// Declare a number variable   // Shortcut for comments is CTRL+/
var loggedCount = 34;
  
console.log("loggedCount");

// Declare a null variable
var x = null;

// Declare an object variable
var person = { name: "John", age: 30 };

// Declare a symbol variable
var id = Symbol();

// Declare an undefined variable
var paymentMode ;
console.log(paymentMode);
paymentMode = "Credit Card";

console.log(fullName);
console.log(isLoggedIn);
console.log(loggedCount); 
console.log(x);
console.log(person);
console.log(id);
console.log(paymentMode);
